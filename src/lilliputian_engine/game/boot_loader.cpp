#include "boot_loader.hpp"

Lilliputian::BootConfiguration* Lilliputian::BootLoader::loadFromFile(String filepath)
{
	BootConfiguration* configuration = new BootConfiguration();

	return configuration;
}